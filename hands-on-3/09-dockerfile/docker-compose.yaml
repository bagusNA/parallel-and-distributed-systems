services:
  app:
    container_name: app
    build:
      dockerfile: 'Dockerfile'
      context: "./app"
    image: "app-golang:1.0.0"
    environment:
      - "APP_PORT=8080"
    ports:
      - "8080:8080"
